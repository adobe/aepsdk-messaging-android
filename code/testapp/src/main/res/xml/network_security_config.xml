<?xml version="1.0" encoding="utf-8"?>
<!--
  Unused — cleartext is controlled via android:usesCleartextTraffic in AndroidManifest.xml.
  Kept for reference if per-domain NSC is needed later (preferred over usesCleartextTraffic on API 28+).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.41.113.178</domain>
    </domain-config>
</network-security-config>
